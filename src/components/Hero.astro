---
import { getImage } from "astro:assets";
import { Image } from 'astro:assets';
import bg from "../assets/bg.png";
import herotext from "./../assets/hero-header.svg"
import headerimg from "../assets/hero-header.svg"
const optimizedBackground = await getImage({
  src: bg,
  format: "webp",
});
---

<div class="h-[80vh] bg-copper-300 flex items-center">
  <section
    class="bg-cover bg-left sm:bg-[100%_60%] w-full h-full"
    style={`background-image: url(${optimizedBackground.src});`}
  >
    <div class="max-w-7xl mx-auto text-left text-gray-900 mt-24">
      <div class="flex items-center">
        <div class="md:w-1/2 px-4">
          <h1 class="text-5xl font-medium mb-6">
            <Image 
            class="mx-auto"
            src={herotext}
            alt="user avatar"
            loading="lazy"
          />
          </h1>
          <p class="text-xl mb-12 max-w-sm">
            We connect you with those people. It's how we make your business
            busier. Success in today's digital world requires more than just a
            great work ethic.
          </p>
          <a
            href="#"
            class="bg-copper-500 text-white font-semibold py-4 px-12 rounded hover:bg-copper-400"
            >Book A Call</a
          >
        </div>
      </div>
    </div>
  </section>
</div>
